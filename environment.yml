channels:
  - conda-forge
  - defaults
dependencies:
  # - conda-export  # Install in base environment when needed.
  - condax
  - ghp-import
  - nodejs
  - panflute
  - pip
  - pipx
  - pypandoc
  - python[version='<3.10']
  - pytz
  - tasklib
  - tzlocal
  - pip:
    - panvimwiki
    - vimwiki-cli
name: vim-python
